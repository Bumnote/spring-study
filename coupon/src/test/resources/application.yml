spring:
  application:
    name: coupon

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  datasource:
    # h2
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password: